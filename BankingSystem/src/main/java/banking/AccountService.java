package banking;

public class AccountService extends Account {

    protected AccountService(AccountHolder accountHolder, Long accountNumber, int pin, double startingDeposit) {
        super(accountHolder, accountNumber, pin, startingDeposit);
    }
    public  void createAccount(){

    }
}
